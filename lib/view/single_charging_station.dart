import 'package:flutter/material.dart';

class SingleChargingStation extends StatelessWidget {
  const SingleChargingStation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}